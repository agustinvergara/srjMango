spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mango_db?serverTimezone=UTC
    username: root
    password: Agus0201.
    driver-class-name: com.mysql.cj.jdbc.Driver
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true